# ── Database (PostgreSQL on VMware/server) ────────────────────────────────────
POSTGRES_HOST=192.168.133.150
POSTGRES_PORT=5432
POSTGRES_DB=srevox
POSTGRES_USER=srevox
POSTGRES_PASSWORD=srevox_dev

# ── Redis (on VMware/server) ──────────────────────────────────────────────────
REDIS_URL=redis://192.168.133.150:6379

# ── API (Node.js Fastify) — port 4000 ─────────────────────────────────────────
API_PORT=4000
BACKEND_SECRET_KEY=change_me_in_production_min_32_chars
ENCRYPTION_KEY=change_me_32_chars_exactly_here_
FRONTEND_URL=http://localhost:3000

# ── AI Service (Python FastAPI) — port 8000 ────────────────────────────────────
AI_SERVICE_URL=http://localhost:8000
AI_PROVIDER=openai
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# ── Alert Worker ──────────────────────────────────────────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ── Frontend (Next.js) — port 3000 ────────────────────────────────────────────
NEXT_PUBLIC_API_URL=http://localhost:4000
